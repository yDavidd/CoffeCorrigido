import { StarRatingDirective } from './star-rating.directive';

describe('StarRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new StarRatingDirective();
    expect(directive).toBeTruthy();
  });
});
